# Brand assets

| File | Use |
|------|-----|
| `SG-Logo_white_400.svg` | Header on navy background (copied to `apps/*/public/sportograf-logo.svg`) |
| `logoblau.svg` | Favicon / PWA maskable icon (`apps/*/public/favicon.svg`) |

Source of truth stays here; apps use copies in `public/`.
